{
  "name": "eval-workers-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types",
    "dryrun": "wrangler deploy --dry-run --outdir=.wrangler/dryrun",
    "db:migrate:local": "wrangler d1 migrations apply eval-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply eval-db --remote",
    "db:seed:local": "wrangler d1 execute eval-db --local --file seed.sql",
    "db:seed:remote": "wrangler d1 execute eval-db --remote --file seed.sql",
    "db:reset:local": "wrangler d1 execute eval-db --local --file reset-local.sql"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.99.0"
  }
}
